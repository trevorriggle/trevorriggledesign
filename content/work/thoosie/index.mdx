---
# COPY. Two sources now, and they are marked where they differ.
#
#   The deck, role, context and state are portfolio-copy.md, verbatim.
#
#   The body is NOT, in two places. "The constraint" is new, and "What I
#   built" gained the build detail this entry was missing next to DrawEvolve's.
#
#   "WHAT I CUT" WAS DELETED RATHER THAN EDITED. It said the Reddit build was
#   finished and deliberately not shipped, and that the standalone web build
#   was the only one that went out. That is no longer true: all three targets
#   shipped off the one codebase, confirmed by the author. A section whose
#   entire argument was a cut that did not happen cannot be corrected a
#   sentence at a time, so it is gone and the three-platform ship took its
#   place. The pull quote it contained about cutting a finished platform went
#   with it, for the same reason.
title: thoosie
deck: Draw a line, ride it as a roller coaster.
section: ai-systems


role:
  - Everything: design, engineering, physics, launch
context: self-initiated
state: Live

# ---- Identity ------------------------------------------------------------
# The mark, in the head. The clip below still leads the page: a logo is
# identity and never stands in for the thing running.
#
# ASPECT IS THE MARK'S CONTENT BOUNDS, NOT THE FILE'S. The upload is a
# 1600x1200 canvas with the wordmark sitting in a band across the middle and
# white above and below it. Declared at 4:3 the mark would render at about a
# third the size of DrawEvolve's and Lynk's tight-cropped files. 1600:700 is
# the band, and the head crops to it, so all three marks carry the same
# optical weight.
#
# The white ground is handled in CSS, not here: the head multiplies the mark
# onto the page so a white-backed export drops out instead of sitting as a
# bright rectangle. See app/work/[slug]/page.module.css.
logo:
  src: logo.jpg
  alt: ""
  aspect: "1600:700"
  minWidth: 1600

# ---- Media ---------------------------------------------------------------
# The clip leads. It renders above everything else on the page, and while the
# mp4 is absent this slot renders `poster` as a still, same box, same
# position, no broken element and no client JavaScript.
#
# THE POSTER IS A FRAME OF THE CLIP, pulled at t=20s, so it is 1920x1080 like
# the video and the box is exactly right in both states: no letterbox when the
# still swaps for the moving picture. It is what a visitor sees whenever
# autoplay is refused, which is most of them, since AutoVideo fails closed on
# any connection it cannot read.
# THE BROWSING THUMBNAIL. Composed for the 4:3 card: the editor on a desktop
# in a real room. The card used to use the clip's poster frame, which is a
# 16:9 action still and lost its own subject to the 4:3 crop.
thumb:
  src: thumbnail.jpg
  alt: "thoosie open in a browser on a desktop monitor on a wooden desk, a finished coaster drawn across the canvas with the element palette down the left and the live ride telemetry panel at the top right"
  aspect: "4:3"
  minWidth: 1600

video:
  src: thoosiecoaster.mp4
  poster:
    src: thoosiecoaster-poster.jpg
    alt: "A thoosie ride in progress: the train cresting the top of a vertical loop on a drawn track, with the live readout showing 14 mph, 0.3 vertical G and airtime counting up, over a summary of 135 ft, 72 mph top speed and three inversions"
    aspect: "16:9"
    minWidth: 1920

# The title screen, which is the other half of what the app looks like: the
# full element palette down the left, the ride telemetry at the top right, and
# a finished coaster drawn behind the wordmark.
images:
  - src: 01.jpg
    alt: "thoosie's title screen, the wordmark and Play, Tutorial and Ride Other's Coasters buttons over a completed coaster, with the drawing tools, track types and prefab elements listed down the left and the ride telemetry panel at the top right"
    aspect: "2400:1230"
    minWidth: 2400

# ABSOLUTE. The second of the two live links on this site.
links:
  - label: Live
    href: https://thoosie.net
    kind: live

# JavaScript, and the three targets it ships to. Named in the copy below, so
# the chips are an index of the prose rather than a claim it does not make.
stack: [JavaScript, Reddit Developer Platform, itch.io, Web]

status: published
---

## The premise

You draw a track freehand. thoosie turns it into a rideable coaster and runs
real physics on it, measuring speed, vertical G, airtime and inversions,
then lets you ride it from the train.

The whole design problem is the gap between those two things. Drawing is
loose and expressive. Physics is unforgiving. A line that looks great is
often a ride that either stalls out halfway up the hill or pulls G-forces
that would put a person in the hospital. The app has to let people draw
freely and still produce something that works.

## The constraint

One codebase, three platforms, and they do not agree about anything.

thoosie ships to the Reddit Developer Platform, to itch.io, and to its own
standalone site at thoosie.net. Those are three genuinely different
environments: Reddit runs the game inside a sandboxed embed on someone
else's page, in a viewport it controls and can resize under you; itch.io
runs it in an iframe with its own fullscreen and sizing rules; the
standalone build owns the whole window and can assume nothing about either
of the other two.

It is all JavaScript, and it is one codebase. A core with three forks looks
the same from outside and rots within a month, because the third platform's
bug fix never makes it back into the first. Everything that differs between
the three is isolated at the
edges: how the canvas learns what size it is, how input arrives, where a
saved coaster is persisted, how fullscreen is requested. The game does not
know which of the three it is running in.

That is the interesting constraint in this project, and it is a design
decision before it is an engineering one. Every feature has to be specified
in a way that survives all three: anything that depends on a known viewport,
on a real URL, or on a window the game controls is either designed
differently or is not built.

## What I built

**A full track editor.** Freehand, straight, and arc tools, plus a library of
prefab elements: first drops, vertical loops, camelbacks, bunny hops,
valleys, barrel rolls, half loops, Immelmanns, dive loops. Track types that
behave differently under the physics sim: chain lift, launch, brake run, and
trigger track.

**A trigger and reaction system**, so pieces can respond to the train passing:
a brake run that holds the train, counts down, then launches it onward, or
track that physically moves between two positions when a trigger fires. That
turned the editor from a drawing tool into something closer to a small
state machine, and it's the feature that made people build things I didn't
anticipate.

**Live physics telemetry surfaced during the ride:** current speed, vertical G,
airtime accumulating in real time, then a summary of height, top speed, max
and min G, inversions, length and ride time.

**Plus the unglamorous half:** autosave, cross-platform level sharing, a
gallery, a tutorial, a content filter on shared coasters, and a full mobile
pass. Cross-platform sharing is the one that earns its place in this list:
a coaster drawn in the Reddit embed opens on the standalone site, which
means the save format had to be settled before any of the three shipped
rather than after two of them had already picked something.

## The decision that mattered

The physics readouts started as debug output. I kept them and made them a
first-class part of the interface, because watching airtime tick up while
you're cresting a hill you drew is the thing that makes the feedback loop
work. Players learn what makes a good coaster by watching the numbers
respond, not by reading a rule.

It's the same instinct as DrawEvolve, honestly. The system's job is to
teach you to get better, not to do it for you.

## Outcome

Launched, on all three platforms, off the one codebase. Promoted through the
coaster and indie dev communities with a single gameplay clip.

The response I actually cared about: two testers played for twenty-five
minutes unprompted, without being asked to and without stopping to give
feedback. For a physics toy with no goals, no score, and no progression,
that's the only signal that means anything.
