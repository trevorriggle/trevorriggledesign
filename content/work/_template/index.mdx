---
# ============================================================================
# CASE STUDY TEMPLATE
# ============================================================================
# Copy this folder to content/work/<slug>/, fill it in, then add <slug> to its
# section's array in content/order.ts. The build fails if you forget.
#
# Every field below is validated. Character caps are deliberate: they make the
# short version the only version that fits, and the short version is the one
# that survives being read by someone with eleven other portfolios open.
#
# There is no roadmap field, no features field and no problem/solution framing.
# Writing one is a build error with an explanation. See content/schema.ts.
# ============================================================================

title: TODO
# One line under the title. A claim about what was built and what it does —
# not a tagline. Max 150 characters.
deck: TODO

# Must be a case-kind section id from content/sections.ts:
#   ai-systems | full-stack
section: ai-systems

year: 2025
role: [TODO]
# client | self-initiated | team | freelance | academic | shelved
context: self-initiated
# collaborators: TODO

# ---- The spine. Five required fields. --------------------------------------

# What limited you. External and specific — a frame budget, a token cost, a
# platform API, a device class, a deadline. Not "users were confused".
constraint: TODO

# Dead ends. Each needs what you tried AND why it failed. At least one.
attempts:
  - tried: TODO
    failed: TODO

# Three parts, because one string lets you write the flattering half and stop.
tradeoff:
  chose: TODO
  instead_of: TODO
  # Every tradeoff cost something. Name it. This is required.
  cost: TODO

outcome:
  what: TODO
  # Optional, and leave it out unless you can defend the number in an
  # interview. An absent metric is fine. An invented one is not.
  # evidence: TODO

# What you would do differently. Required — and the one field you cannot fill
# without conceding something didn't go well.
revisit: TODO

# ---- Optional structure for AI work ----------------------------------------
# Delete the blocks that don't apply. Each renders only when present.

# architecture:
#   caption: TODO
#   stages:
#     - id: app
#       label: TODO
#       kind: client        # client | edge | service | model | store | job
#       detail: TODO
#     - id: llm
#       label: TODO
#       kind: model
#   flow:
#     - app -> llm
#   # Optional drawn version. The stage table above renders without it.
#   diagram:
#     src: 00-architecture.png
#     alt: TODO
#     aspect: "16:9"
#     minWidth: 2000
#     label: TODO — what must this image show?

# The model/provider row.
# models:
#   - role: TODO
#     provider: TODO
#     model: TODO
#     why: TODO

# The latency-or-cost constraint you actually built against.
# budget:
#   metric: latency        # latency | cost | throughput | memory | frame-time
#   target: TODO
#   measured: TODO         # omit rather than estimate
#   method: TODO           # how it was measured — an unqualified number isn't evidence

# What the system does when the model is wrong. The question technical
# interviewers actually ask.
# failureModes:
#   - when: TODO
#     then: TODO
#     surfaced: TODO

# ---- Media -----------------------------------------------------------------
# Files go in public/media/<slug>/. Nothing needs to exist yet: a declared
# image with no file renders as a spec placeholder carrying this metadata.
# Drop the real file at that path and it takes over with no other edit.

cover:
  src: 01-cover.png
  alt: TODO
  aspect: "3:2"
  minWidth: 2400
  label: TODO — what must this image show?

images:
  - src: 02-detail.png
    alt: TODO
    aspect: "3:2"
    minWidth: 1800
    label: TODO — what must this image show?
    # caption: TODO
    # bleed: true          # break the column, run to the wide measure

# External links. Absolute https:// only — a relative path fails the build,
# which is the fix for every "Live demo" link that 404'd on the old site.
links: []
#   - label: Live
#     href: https://example.com
#     kind: live           # live | repo | writing | press | video | other

stack: [TODO]

# draft renders in dev and on preview deploys only.
status: draft
---

{/* The body is optional and takes MDX. It is for the detail that doesn't fit
    in the capped frontmatter fields — the actual walk through the work.

    The frontmatter above is the argument. Don't restate it here; go deeper
    into one or two moves instead. Available components:

      <Figure src="02-detail.png" caption="Optional override" />
      <Aside>A digression that shouldn't interrupt the main line.</Aside>
      <Compare>
        <Column label="Rejected">…</Column>
        <Column label="Shipped">…</Column>
      </Compare>

    A <Figure> src must be declared in images[] above, or the build fails —
    that's what guarantees every image has alt text. */}

TODO
